package controllers

import (
	"github.com/gin-gonic/gin"
	"net/http"
)

func Page(context *gin.Context) {
	context.JSON(http.StatusOK, gin.H{"data": "This is a page served from the STUB controller."})
}